version: "2"
services:
    justuse-scraper:
        build: .
        image: challisa/justuse-scraper
        container_name: justuse-scraper
        volumes:
            - .:/opt/working
        command: tail -f /dev/null
